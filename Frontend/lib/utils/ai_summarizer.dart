// Future<String> summarizeMarkdownFile(String filePath) async {
//   final content = await File(filePath).readAsString();

//   // TODO: 실제 AI 요약 모델에 API 호출 구현 (예시용 더미 텍스트)
//   final summary = '이 웹페이지의 요약: \n\n' + content.split('\n').take(5).join('\n');
//   return summary;
// }
